#include <stdio.h>

int main (){ 
int anio;
int sala;
int por;

   printf("Ingresa tu antiguedad");
       scanf("%d", &anio);
   if(anio >=5)
   printf("Ingrese salario");
   scanf("%d", &sala);
     if((sala*100)/10>1000)
     {printf("Felicidades, tu creddi ha sido aprobado");}
  }
  